apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/almerico/activiti-cloud-application-chart:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/almerico/activiti-cloud-application-chart:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: activiti-cloud-application-chart
        chartPath: charts/activiti-cloud-application-chart
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/almerico/activiti-cloud-application-chart"
          image.tag: "{{.DIGEST_HEX}}"
        
